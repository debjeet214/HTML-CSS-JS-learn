let a = prompt("Enter first number: ")
let b = prompt("Enter second number: ")

let stringaddition = a+b;   // this will just append the two string into result
let sum = parseInt(a) + parseInt(b)  // this will first convert the input num-string into integer & the add

console.log("The sum is" , sum);
console.log("Appended form is ", stringaddition)
