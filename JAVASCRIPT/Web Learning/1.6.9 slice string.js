// If you want to cut out or slice out a part of the given string, then you can do so using the slice() method.

let a = "Orange Banana Mango";
let subA = a.slice(7,14);   //result of subA will be Banana 
console.log(subA)  // it will output after 7th to 14th no. string index
