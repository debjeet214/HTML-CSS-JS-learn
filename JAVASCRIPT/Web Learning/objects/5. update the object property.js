let mobile = {
	brand: "Apple",
	model : "iPhone 12",
	colors: "black"
};

var colors =["Blue", "Green", "Black", "White", "Purple"];    
mobile.colors = colors;    // we can assign an array as the property value as multiple options

console.log(mobile);
